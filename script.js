// require - a buit in function to include external modules that exist in separate files
// const math = require("./math");
// console.log(math);



/* exporting data from separate directory. */

const info = require("./fruits");
console.log(info);
